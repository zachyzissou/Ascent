#include "ClimbingPerformanceManager.h"
#include "AdvancedRopeComponent.h"
#include "../Player/AdvancedClimbingComponent.h"
#include "../Tools/ClimbingToolBase.h"
#include "Engine/World.h"
#include "GameFramework/PlayerController.h"
#include "Kismet/GameplayStatics.h"

// Integration methods for ClimbingPerformanceManager with physics systems

void UClimbingPerformanceManager::UpdatePerformanceMetrics(float DeltaTime)
{
    if (!CachedWorld)
        return;

    // Update object counts
    CurrentMetrics.ActiveRopes = TrackedRopes.Num();
    CurrentMetrics.ActiveTools = TrackedTools.Num();
    CurrentMetrics.ActiveClimbers = TrackedClimbers.Num();
    
    // Count physics constraints
    CurrentMetrics.PhysicsConstraints = 0;
    for (UAdvancedRopeComponent* Rope : TrackedRopes)
    {
        if (IsValid(Rope) && Rope->CableComponent)
        {
            CurrentMetrics.PhysicsConstraints += Rope->CableComponent->NumSegments;
        }
    }
    
    // Update physics timing
    PhysicsTimeAccumulator += DeltaTime;
    PhysicsFrameCounter++;
    
    if (PhysicsFrameCounter >= 60) // Update every 60 frames
    {
        CurrentMetrics.PhysicsTime = (PhysicsTimeAccumulator / PhysicsFrameCounter) * 1000.0f;
        PhysicsTimeAccumulator = 0.0f;
        PhysicsFrameCounter = 0;
    }
    
    // Update memory usage
    TrackMemoryUsage();
    
    // Update render metrics (simplified)
    CurrentMetrics.DrawCalls = GEngine->GetCurrentDrawCalls();
    CurrentMetrics.Triangles = GEngine->GetCurrentTriangleCount();
    
    // Update network metrics if in multiplayer
    if (CachedWorld->GetNetMode() != NM_Standalone)
    {
        CurrentMetrics.NetworkBandwidthKBps = GetNetworkBandwidth();
        CurrentMetrics.NetworkLatencyMs = GetNetworkLatency();
    }
    
    // Update statistics
    SET_DWORD_STAT(STAT_ActiveRopes, CurrentMetrics.ActiveRopes);
    SET_DWORD_STAT(STAT_PhysicsConstraints, CurrentMetrics.PhysicsConstraints);
}

void UClimbingPerformanceManager::UpdateLODSystem(float DeltaTime)
{
    LastLODUpdate += DeltaTime;
    if (LastLODUpdate < LODUpdateInterval)
        return;
    
    LastLODUpdate = 0.0f;
    
    FVector AverageViewerLocation = GetAverageViewerLocation();
    
    // Update rope LODs
    for (UAdvancedRopeComponent* Rope : TrackedRopes)
    {
        if (IsValid(Rope))
        {
            EPerformanceLOD NewLOD = GetObjectLOD(Rope->GetOwner(), AverageViewerLocation);
            EPerformanceLOD* CachedLOD = ActorLODCache.Find(Rope->GetOwner());
            
            if (!CachedLOD || *CachedLOD != NewLOD)
            {
                OptimizeRopePhysics(Rope, NewLOD);
                ActorLODCache.Add(Rope->GetOwner(), NewLOD);
            }
        }
    }
    
    // Update climber LODs
    for (UAdvancedClimbingComponent* Climber : TrackedClimbers)
    {
        if (IsValid(Climber))
        {
            EPerformanceLOD NewLOD = GetObjectLOD(Climber->GetOwner(), AverageViewerLocation);
            EPerformanceLOD* CachedLOD = ActorLODCache.Find(Climber->GetOwner());
            
            if (!CachedLOD || *CachedLOD != NewLOD)
            {
                OptimizeClimberPhysics(Climber, NewLOD);
                ActorLODCache.Add(Climber->GetOwner(), NewLOD);
            }
        }
    }
    
    // Update tool LODs
    for (AClimbingToolBase* Tool : TrackedTools)
    {
        if (IsValid(Tool))
        {
            EPerformanceLOD NewLOD = GetObjectLOD(Tool, AverageViewerLocation);
            EPerformanceLOD* CachedLOD = ActorLODCache.Find(Tool);
            \n            if (!CachedLOD || *CachedLOD != NewLOD)\n            {\n                OptimizeToolPhysics(Tool, NewLOD);\n                ActorLODCache.Add(Tool, NewLOD);\n            }\n        }\n    }\n}\n\nvoid UClimbingPerformanceManager::OptimizeRopePhysics(UAdvancedRopeComponent* Rope, EPerformanceLOD LOD)\n{\n    if (!IsValid(Rope) || !Rope->CableComponent)\n        return;\n    \n    SCOPE_CYCLE_COUNTER(STAT_RopePhysics);\n    \n    switch (LOD)\n    {\n        case EPerformanceLOD::Ultra:\n            ApplyRopeLOD_Ultra(Rope);\n            break;\n        case EPerformanceLOD::High:\n            ApplyRopeLOD_High(Rope);\n            break;\n        case EPerformanceLOD::Medium:\n            ApplyRopeLOD_Medium(Rope);\n            break;\n        case EPerformanceLOD::Low:\n            ApplyRopeLOD_Low(Rope);\n            break;\n        case EPerformanceLOD::Minimal:\n            ApplyRopeLOD_Minimal(Rope);\n            break;\n        case EPerformanceLOD::Disabled:\n            ApplyRopeLOD_Disabled(Rope);\n            break;\n    }\n}\n\nvoid UClimbingPerformanceManager::OptimizeClimberPhysics(UAdvancedClimbingComponent* Climber, EPerformanceLOD LOD)\n{\n    if (!IsValid(Climber))\n        return;\n    \n    SCOPE_CYCLE_COUNTER(STAT_ClimbingPhysics);\n    \n    switch (LOD)\n    {\n        case EPerformanceLOD::Ultra:\n        case EPerformanceLOD::High:\n            // Full precision climbing physics\n            Climber->GripUpdateInterval = 0.1f;\n            Climber->StaminaUpdateInterval = 0.2f;\n            Climber->GripScanInterval = 0.5f;\n            Climber->bOptimizeDistantUpdates = false;\n            break;\n            \n        case EPerformanceLOD::Medium:\n            // Moderate precision\n            Climber->GripUpdateInterval = 0.15f;\n            Climber->StaminaUpdateInterval = 0.3f;\n            Climber->GripScanInterval = 1.0f;\n            Climber->bOptimizeDistantUpdates = true;\n            break;\n            \n        case EPerformanceLOD::Low:\n            // Reduced precision\n            Climber->GripUpdateInterval = 0.2f;\n            Climber->StaminaUpdateInterval = 0.5f;\n            Climber->GripScanInterval = 2.0f;\n            Climber->bOptimizeDistantUpdates = true;\n            break;\n            \n        case EPerformanceLOD::Minimal:\n            // Minimal updates\n            Climber->GripUpdateInterval = 0.5f;\n            Climber->StaminaUpdateInterval = 1.0f;\n            Climber->GripScanInterval = 5.0f;\n            Climber->bOptimizeDistantUpdates = true;\n            break;\n            \n        case EPerformanceLOD::Disabled:\n            // Disable non-essential systems\n            Climber->GripUpdateInterval = 1.0f;\n            Climber->StaminaUpdateInterval = 2.0f;\n            Climber->GripScanInterval = 10.0f;\n            Climber->bOptimizeDistantUpdates = true;\n            break;\n    }\n}\n\nvoid UClimbingPerformanceManager::OptimizeToolPhysics(AClimbingToolBase* Tool, EPerformanceLOD LOD)\n{\n    if (!IsValid(Tool))\n        return;\n    \n    UStaticMeshComponent* MeshComp = Tool->GetStaticMeshComponent();\n    if (!MeshComp)\n        return;\n    \n    switch (LOD)\n    {\n        case EPerformanceLOD::Ultra:\n        case EPerformanceLOD::High:\n            // Full physics simulation\n            MeshComp->SetSimulatePhysics(true);\n            MeshComp->SetCollisionEnabled(ECollisionEnabled::QueryAndPhysics);\n            break;\n            \n        case EPerformanceLOD::Medium:\n            // Simplified physics\n            MeshComp->SetSimulatePhysics(true);\n            MeshComp->SetCollisionEnabled(ECollisionEnabled::QueryOnly);\n            break;\n            \n        case EPerformanceLOD::Low:\n        case EPerformanceLOD::Minimal:\n            // Static representation\n            MeshComp->SetSimulatePhysics(false);\n            MeshComp->SetCollisionEnabled(ECollisionEnabled::QueryOnly);\n            break;\n            \n        case EPerformanceLOD::Disabled:\n            // No collision\n            MeshComp->SetSimulatePhysics(false);\n            MeshComp->SetCollisionEnabled(ECollisionEnabled::NoCollision);\n            break;\n    }\n}\n\nvoid UClimbingPerformanceManager::BatchUpdateRopes(float DeltaTime)\n{\n    if (TrackedRopes.Num() == 0)\n        return;\n    \n    // Process a batch of ropes each frame to spread the load\n    int32 StartIndex = RopeBatchIndex;\n    int32 EndIndex = FMath::Min(StartIndex + BatchSize, TrackedRopes.Num());\n    \n    for (int32 i = StartIndex; i < EndIndex; ++i)\n    {\n        if (IsValid(TrackedRopes[i]))\n        {\n            ProcessRopeOptimization(TrackedRopes[i], DeltaTime);\n        }\n        else\n        {\n            // Remove invalid rope\n            TrackedRopes.RemoveAtSwap(i);\n            i--; // Adjust index for removed element\n            EndIndex--;\n        }\n    }\n    \n    // Move to next batch\n    RopeBatchIndex = (EndIndex >= TrackedRopes.Num()) ? 0 : EndIndex;\n}\n\nvoid UClimbingPerformanceManager::BatchUpdateTools(float DeltaTime)\n{\n    if (TrackedTools.Num() == 0)\n        return;\n    \n    int32 StartIndex = ToolBatchIndex;\n    int32 EndIndex = FMath::Min(StartIndex + BatchSize, TrackedTools.Num());\n    \n    for (int32 i = StartIndex; i < EndIndex; ++i)\n    {\n        if (IsValid(TrackedTools[i]))\n        {\n            ProcessToolOptimization(TrackedTools[i], DeltaTime);\n        }\n        else\n        {\n            TrackedTools.RemoveAtSwap(i);\n            i--;\n            EndIndex--;\n        }\n    }\n    \n    ToolBatchIndex = (EndIndex >= TrackedTools.Num()) ? 0 : EndIndex;\n}\n\nvoid UClimbingPerformanceManager::BatchUpdateClimbers(float DeltaTime)\n{\n    if (TrackedClimbers.Num() == 0)\n        return;\n    \n    int32 StartIndex = ClimberBatchIndex;\n    int32 EndIndex = FMath::Min(StartIndex + BatchSize, TrackedClimbers.Num());\n    \n    for (int32 i = StartIndex; i < EndIndex; ++i)\n    {\n        if (IsValid(TrackedClimbers[i]))\n        {\n            ProcessClimberOptimization(TrackedClimbers[i], DeltaTime);\n        }\n        else\n        {\n            TrackedClimbers.RemoveAtSwap(i);\n            i--;\n            EndIndex--;\n        }\n    }\n    \n    ClimberBatchIndex = (EndIndex >= TrackedClimbers.Num()) ? 0 : EndIndex;\n}\n\nvoid UClimbingPerformanceManager::AdjustQualityForPerformance()\n{\n    if (FrameTimeHistory.Num() < 60) // Need at least 60 frames of data\n        return;\n    \n    // Calculate recent performance\n    float RecentAverageFrameTime = 0.0f;\n    int32 RecentFrames = FMath::Min(60, FrameTimeHistory.Num());\n    \n    for (int32 i = FrameTimeHistory.Num() - RecentFrames; i < FrameTimeHistory.Num(); ++i)\n    {\n        RecentAverageFrameTime += FrameTimeHistory[i];\n    }\n    RecentAverageFrameTime /= RecentFrames;\n    \n    float RecentFPS = 1000.0f / RecentAverageFrameTime;\n    \n    // Determine if quality adjustment is needed\n    bool bNeedsReduction = RecentFPS < PerformanceTargets.MinimumFPS;\n    bool bCanIncrease = RecentFPS > PerformanceTargets.TargetFPS * 1.1f; // 10% above target\n    \n    if (bNeedsReduction)\n    {\n        ReduceQuality();\n    }\n    else if (bCanIncrease)\n    {\n        IncreaseQuality();\n    }\n}\n\nvoid UClimbingPerformanceManager::ReduceQuality()\n{\n    // Reduce rope segment counts globally\n    PhysicsSettings.UltraRopeSegments = FMath::Max(32, PhysicsSettings.UltraRopeSegments - 8);\n    PhysicsSettings.HighRopeSegments = FMath::Max(16, PhysicsSettings.HighRopeSegments - 4);\n    PhysicsSettings.MediumRopeSegments = FMath::Max(8, PhysicsSettings.MediumRopeSegments - 2);\n    PhysicsSettings.LowRopeSegments = FMath::Max(4, PhysicsSettings.LowRopeSegments - 1);\n    \n    // Reduce solver iterations\n    PhysicsSettings.UltraSolverIterations = FMath::Max(4, PhysicsSettings.UltraSolverIterations - 1);\n    PhysicsSettings.HighSolverIterations = FMath::Max(3, PhysicsSettings.HighSolverIterations - 1);\n    PhysicsSettings.MediumSolverIterations = FMath::Max(2, PhysicsSettings.MediumSolverIterations - 1);\n    \n    // Reduce update rates\n    PhysicsSettings.UltraUpdateRate = FMath::Max(30.0f, PhysicsSettings.UltraUpdateRate - 10.0f);\n    PhysicsSettings.HighUpdateRate = FMath::Max(20.0f, PhysicsSettings.HighUpdateRate - 5.0f);\n    \n    // Apply changes to active ropes\n    for (UAdvancedRopeComponent* Rope : TrackedRopes)\n    {\n        if (IsValid(Rope))\n        {\n            EPerformanceLOD* CurrentLOD = ActorLODCache.Find(Rope->GetOwner());\n            if (CurrentLOD)\n            {\n                OptimizeRopePhysics(Rope, *CurrentLOD);\n            }\n        }\n    }\n    \n    OnQualityAdjusted.Broadcast();\n    UE_LOG(LogTemp, Warning, TEXT(\"Performance: Quality reduced due to low framerate\"));\n}\n\nvoid UClimbingPerformanceManager::IncreaseQuality()\n{\n    // Only increase if we're not already at max\n    bool bCanIncrease = PhysicsSettings.UltraRopeSegments < 64 || \n                       PhysicsSettings.UltraSolverIterations < 8;\n    \n    if (!bCanIncrease)\n        return;\n    \n    // Increase rope segment counts\n    PhysicsSettings.UltraRopeSegments = FMath::Min(64, PhysicsSettings.UltraRopeSegments + 8);\n    PhysicsSettings.HighRopeSegments = FMath::Min(32, PhysicsSettings.HighRopeSegments + 4);\n    PhysicsSettings.MediumRopeSegments = FMath::Min(16, PhysicsSettings.MediumRopeSegments + 2);\n    \n    // Increase solver iterations\n    PhysicsSettings.UltraSolverIterations = FMath::Min(8, PhysicsSettings.UltraSolverIterations + 1);\n    PhysicsSettings.HighSolverIterations = FMath::Min(6, PhysicsSettings.HighSolverIterations + 1);\n    \n    // Apply changes\n    for (UAdvancedRopeComponent* Rope : TrackedRopes)\n    {\n        if (IsValid(Rope))\n        {\n            EPerformanceLOD* CurrentLOD = ActorLODCache.Find(Rope->GetOwner());\n            if (CurrentLOD)\n            {\n                OptimizeRopePhysics(Rope, *CurrentLOD);\n            }\n        }\n    }\n    \n    OnQualityAdjusted.Broadcast();\n    UE_LOG(LogTemp, Log, TEXT(\"Performance: Quality increased due to stable high framerate\"));\n}\n\nvoid UClimbingPerformanceManager::CheckPerformanceThresholds()\n{\n    bool bPerformanceWarning = false;\n    bool bPerformanceCritical = false;\n    \n    // Check frame time\n    if (CurrentMetrics.AverageFrameTime > PerformanceTargets.MaxFrameTimeMs * 1.5f)\n    {\n        bPerformanceCritical = true;\n    }\n    else if (CurrentMetrics.AverageFrameTime > PerformanceTargets.MaxFrameTimeMs * 1.2f)\n    {\n        bPerformanceWarning = true;\n    }\n    \n    // Check physics time\n    if (CurrentMetrics.PhysicsTime > PerformanceTargets.MaxPhysicsTimeMs * 2.0f)\n    {\n        bPerformanceCritical = true;\n    }\n    else if (CurrentMetrics.PhysicsTime > PerformanceTargets.MaxPhysicsTimeMs * 1.5f)\n    {\n        bPerformanceWarning = true;\n    }\n    \n    // Check memory usage\n    if (CurrentMetrics.MemoryUsageMB > PerformanceTargets.MaxMemoryUsageMB * 0.9f)\n    {\n        bPerformanceWarning = true;\n    }\n    \n    // Check object counts\n    if (CurrentMetrics.ActiveRopes > PerformanceTargets.MaxActiveRopes)\n    {\n        bPerformanceWarning = true;\n    }\n    \n    if (CurrentMetrics.PhysicsConstraints > PerformanceTargets.MaxPhysicsConstraints)\n    {\n        bPerformanceCritical = true;\n    }\n    \n    // Broadcast events\n    if (bPerformanceCritical)\n    {\n        OnPerformanceCritical.Broadcast();\n        UE_LOG(LogTemp, Error, TEXT(\"Critical performance issues detected!\"));\n    }\n    else if (bPerformanceWarning)\n    {\n        OnPerformanceWarning.Broadcast();\n        UE_LOG(LogTemp, Warning, TEXT(\"Performance warning: System under stress\"));\n    }\n}\n\n// Helper methods for rope LOD application\nvoid UClimbingPerformanceManager::ApplyRopeLOD_Ultra(UAdvancedRopeComponent* Rope)\n{\n    if (!Rope->CableComponent) return;\n    \n    Rope->CableComponent->NumSegments = PhysicsSettings.UltraRopeSegments;\n    Rope->CableComponent->SolverIterations = PhysicsSettings.UltraSolverIterations;\n    Rope->CableComponent->SubstepTime = 1.0f / PhysicsSettings.UltraUpdateRate;\n    Rope->CableComponent->bUseSubstepping = true;\n    Rope->CableComponent->bEnableCollision = true;\n    Rope->CableComponent->SetSimulatePhysics(true);\n}\n\nvoid UClimbingPerformanceManager::ApplyRopeLOD_High(UAdvancedRopeComponent* Rope)\n{\n    if (!Rope->CableComponent) return;\n    \n    Rope->CableComponent->NumSegments = PhysicsSettings.HighRopeSegments;\n    Rope->CableComponent->SolverIterations = PhysicsSettings.HighSolverIterations;\n    Rope->CableComponent->SubstepTime = 1.0f / PhysicsSettings.HighUpdateRate;\n    Rope->CableComponent->bUseSubstepping = true;\n    Rope->CableComponent->bEnableCollision = true;\n    Rope->CableComponent->SetSimulatePhysics(true);\n}\n\nvoid UClimbingPerformanceManager::ApplyRopeLOD_Medium(UAdvancedRopeComponent* Rope)\n{\n    if (!Rope->CableComponent) return;\n    \n    Rope->CableComponent->NumSegments = PhysicsSettings.MediumRopeSegments;\n    Rope->CableComponent->SolverIterations = PhysicsSettings.MediumSolverIterations;\n    Rope->CableComponent->SubstepTime = 1.0f / PhysicsSettings.MediumUpdateRate;\n    Rope->CableComponent->bUseSubstepping = false;\n    Rope->CableComponent->bEnableCollision = true;\n    Rope->CableComponent->SetSimulatePhysics(true);\n}\n\nvoid UClimbingPerformanceManager::ApplyRopeLOD_Low(UAdvancedRopeComponent* Rope)\n{\n    if (!Rope->CableComponent) return;\n    \n    Rope->CableComponent->NumSegments = PhysicsSettings.LowRopeSegments;\n    Rope->CableComponent->SolverIterations = PhysicsSettings.LowSolverIterations;\n    Rope->CableComponent->SubstepTime = 1.0f / PhysicsSettings.LowUpdateRate;\n    Rope->CableComponent->bUseSubstepping = false;\n    Rope->CableComponent->bEnableCollision = false;\n    Rope->CableComponent->SetSimulatePhysics(true);\n}\n\nvoid UClimbingPerformanceManager::ApplyRopeLOD_Minimal(UAdvancedRopeComponent* Rope)\n{\n    if (!Rope->CableComponent) return;\n    \n    Rope->CableComponent->NumSegments = PhysicsSettings.MinimalRopeSegments;\n    Rope->CableComponent->SolverIterations = PhysicsSettings.MinimalSolverIterations;\n    Rope->CableComponent->SubstepTime = 1.0f / PhysicsSettings.MinimalUpdateRate;\n    Rope->CableComponent->bUseSubstepping = false;\n    Rope->CableComponent->bEnableCollision = false;\n    Rope->CableComponent->SetSimulatePhysics(true);\n}\n\nvoid UClimbingPerformanceManager::ApplyRopeLOD_Disabled(UAdvancedRopeComponent* Rope)\n{\n    if (!Rope->CableComponent) return;\n    \n    Rope->CableComponent->SetSimulatePhysics(false);\n    Rope->CableComponent->bEnableCollision = false;\n    Rope->CableComponent->NumSegments = 2; // Minimum for line rendering\n}\n\n// Additional helper methods\nvoid UClimbingPerformanceManager::ProcessRopeOptimization(UAdvancedRopeComponent* Rope, float DeltaTime)\n{\n    if (!IsValid(Rope) || !Rope->CableComponent)\n        return;\n    \n    // Update rope-specific optimizations\n    float ViewerDistance = GetDistanceToNearestViewer(Rope->GetOwner());\n    \n    // Cache distance for LOD calculations\n    ActorDistanceCache.Add(Rope->GetOwner(), ViewerDistance);\n    \n    // Apply distance-based optimizations\n    if (ViewerDistance > LODDistances.DisabledDistance)\n    {\n        if (Rope->CableComponent->IsSimulatingPhysics())\n        {\n            ApplyRopeLOD_Disabled(Rope);\n        }\n    }\n}\n\nvoid UClimbingPerformanceManager::ProcessToolOptimization(AClimbingToolBase* Tool, float DeltaTime)\n{\n    if (!IsValid(Tool))\n        return;\n    \n    float ViewerDistance = GetDistanceToNearestViewer(Tool);\n    ActorDistanceCache.Add(Tool, ViewerDistance);\n    \n    // Tools can be made static at distance\n    if (ViewerDistance > LODDistances.LowDistance)\n    {\n        UStaticMeshComponent* MeshComp = Tool->GetStaticMeshComponent();\n        if (MeshComp && MeshComp->IsSimulatingPhysics())\n        {\n            MeshComp->SetSimulatePhysics(false);\n        }\n    }\n}\n\nvoid UClimbingPerformanceManager::ProcessClimberOptimization(UAdvancedClimbingComponent* Climber, float DeltaTime)\n{\n    if (!IsValid(Climber))\n        return;\n    \n    float ViewerDistance = GetDistanceToNearestViewer(Climber->GetOwner());\n    ActorDistanceCache.Add(Climber->GetOwner(), ViewerDistance);\n    \n    // Update climber's internal distance tracking for self-optimization\n    Climber->PlayerViewDistance = ViewerDistance;\n}\n\nfloat UClimbingPerformanceManager::GetDistanceToNearestViewer(AActor* Actor) const\n{\n    if (!Actor || !CachedWorld)\n        return FLT_MAX;\n    \n    float MinDistance = FLT_MAX;\n    FVector ActorLocation = Actor->GetActorLocation();\n    \n    // Check all player controllers\n    for (FConstPlayerControllerIterator Iterator = CachedWorld->GetPlayerControllerIterator(); Iterator; ++Iterator)\n    {\n        APlayerController* PC = Iterator->Get();\n        if (PC && PC->GetPawn())\n        {\n            FVector ViewerLocation = PC->GetPawn()->GetActorLocation();\n            float Distance = FVector::Dist(ActorLocation, ViewerLocation);\n            MinDistance = FMath::Min(MinDistance, Distance);\n        }\n    }\n    \n    return MinDistance;\n}\n\nFVector UClimbingPerformanceManager::GetAverageViewerLocation() const\n{\n    if (!CachedWorld)\n        return FVector::ZeroVector;\n    \n    FVector AverageLocation = FVector::ZeroVector;\n    int32 ViewerCount = 0;\n    \n    for (FConstPlayerControllerIterator Iterator = CachedWorld->GetPlayerControllerIterator(); Iterator; ++Iterator)\n    {\n        APlayerController* PC = Iterator->Get();\n        if (PC && PC->GetPawn())\n        {\n            AverageLocation += PC->GetPawn()->GetActorLocation();\n            ViewerCount++;\n        }\n    }\n    \n    if (ViewerCount > 0)\n    {\n        AverageLocation /= ViewerCount;\n    }\n    \n    return AverageLocation;\n}\n\nEPerformanceLOD UClimbingPerformanceManager::CalculateLODFromDistance(float Distance) const\n{\n    if (Distance <= LODDistances.UltraDistance)\n        return EPerformanceLOD::Ultra;\n    else if (Distance <= LODDistances.HighDistance)\n        return EPerformanceLOD::High;\n    else if (Distance <= LODDistances.MediumDistance)\n        return EPerformanceLOD::Medium;\n    else if (Distance <= LODDistances.LowDistance)\n        return EPerformanceLOD::Low;\n    else if (Distance <= LODDistances.MinimalDistance)\n        return EPerformanceLOD::Minimal;\n    else\n        return EPerformanceLOD::Disabled;\n}\n\nvoid UClimbingPerformanceManager::TrackMemoryUsage()\n{\n    // Simplified memory tracking - in a full implementation would use more detailed memory stats\n    FPlatformMemoryStats MemStats = FPlatformMemory::GetStats();\n    CurrentMetrics.MemoryUsageMB = MemStats.UsedPhysical / (1024.0f * 1024.0f);\n}\n\nfloat UClimbingPerformanceManager::GetNetworkBandwidth() const\n{\n    // Simplified network metrics - would be enhanced with proper network statistics\n    return 0.0f; // Placeholder\n}\n\nfloat UClimbingPerformanceManager::GetNetworkLatency() const\n{\n    // Simplified latency calculation\n    if (CachedWorld->GetFirstPlayerController())\n    {\n        return CachedWorld->GetFirstPlayerController()->PlayerState ? \n               CachedWorld->GetFirstPlayerController()->PlayerState->GetPing() : 0.0f;\n    }\n    return 0.0f;\n}"